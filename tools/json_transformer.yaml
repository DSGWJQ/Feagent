name: json_transformer
description: Transform JSON input via JavaScript.
category: custom
version: "1.0.0"
parameters:
  - name: input
    type: any
    description: Input payload.
    required: true
entry:
  type: javascript
  code: |
    function transform(input) {
      return input;
    }
returns:
  type: any
  description: Transformed payload.
