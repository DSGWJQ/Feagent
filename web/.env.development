# API 基础 URL
# 开发环境留空，使用 Vite 代理（见 vite.config.ts）
# 生产环境设置为实际的后端地址
VITE_API_BASE_URL=

# 应用标题
VITE_APP_TITLE=Agent 中台系统

# 是否启用 Mock 模式
# true: 使用前端模拟数据，无需启动后端（适合前端开发）
# false: 连接真实后端 API（需要先启动后端服务）
#
# 启动后端命令：
# uvicorn src.interfaces.api.main:app --reload --port 8000
#
VITE_USE_MOCK=true
