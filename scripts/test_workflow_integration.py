"""Moved to ``tests/manual/test_workflow_integration.py``.

The executable copy lives with the other manual end-to-end workflow scripts.
This stub stays here only because deleting files inside ``agent_data`` is not
permitted in the current automation sandbox.
"""
