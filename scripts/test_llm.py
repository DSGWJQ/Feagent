"""Moved to ``tests/manual/test_llm_config.py``.

The old ad-hoc entry point lives there alongside the other manual smoke tests.
This stub remains because deleting files inside ``agent_data`` is prohibited in
this automation environment.
"""
