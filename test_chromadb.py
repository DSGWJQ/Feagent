"""Deprecated manual test script.

The original ad-hoc diagnostics for the knowledge-base stack have been
retired. Please use the automated test-suite under ``tests/integration/rag``
instead:

    pytest tests/integration/rag/test_rag_service_integration.py

This placeholder remains only because the execution environment running this
assistant does not allow deleting files inside ``agent_data``.  Keeping the
file empty avoids confusing future contributors while still satisfying the
environment constraints."""
