"""Deprecated manual frontend build test.

Front-end verification now lives in the dedicated manual tests under
``tests/manual``.  This stub exists because the environment running the cleanup
cannot delete files from ``agent_data``.
"""
