# Example env for deterministic Playwright E2E (Mode A).
# Copy to `.env.test` to override locally.

ENV=test
LOG_LEVEL=INFO
LOG_FORMAT=text

# Local SQLite DB for e2e runs (relative to repo root).
DATABASE_URL=sqlite+aiosqlite:///./agent_platform_e2e.db

# Deterministic: Stub LLM + Mock HTTP (offline).
E2E_TEST_MODE=deterministic
LLM_ADAPTER=stub
HTTP_ADAPTER=mock

# Enable test-only seed/cleanup APIs used by the Playwright fixtures.
ENABLE_TEST_SEED_API=true

# Enable Runs API paths in deterministic suite.
DISABLE_RUN_PERSISTENCE=false

# Keep feature flags fail-closed by default.
ENABLE_DECISION_EXECUTION_BRIDGE=false
ENABLE_INTERNAL_WORKFLOW_CREATE_ENDPOINTS=false

# Dev-only: default JWT secret for tests (override in real deployments).
SECRET_KEY=your-secret-key-change-this-in-production
